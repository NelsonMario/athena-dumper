from pypika import Table

# Define a Table object for the 'groups' table in the 'database_bar' schema
groups = Table("database_bar.groups")

# Define a Table object for the 'users' table in the 'database_bar' schema
users = Table("database_bar.users")
